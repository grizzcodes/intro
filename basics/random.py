from adder import adding, count, substract as sub, equations
                                    # can be intertwined to = same thing

# from the module, import the following functions
adding(3,3)
count("Alec")
sub(20,3)   # substract = sub when importing
equations(9)